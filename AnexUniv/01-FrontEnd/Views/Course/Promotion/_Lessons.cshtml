<div class="panel panel-default">
    <div class="panel-heading">
        <h2>Temario <small>10 lecciones</small></h2>
    </div>
    <ul class="list-group">
        @for (var y = 1; y < 4; y++)
        {
        <li class="list-group-item">
            <b>#0@(y)</b> Como construir un nuevo curso sobre metodologías ágiles

            <span class="badge">
                @if (y % 2 == 0)
                {
                    <i data-toggle="tooltip" data-placement="top" title="Video" class="fa fa-youtube-play"></i>
                }
                else
                {
                    <i data-toggle="tooltip" data-placement="top" title="Lectura" class="fa fa-book"></i>
                }
            </span>
        </li>
        }
    </ul>
</div>